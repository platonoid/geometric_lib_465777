import math


def area(r):
    #Принимает число r, возвращает площадь круга с радиусом r
    #При r = 2 вернёт pi*4
    return math.pi * r * r


def perimeter(r):
    #Принимает число r, возвращает периметр крга с радиусом r
    #При r = 3, вернёт pi*6
    return 2 * math.pi * r

